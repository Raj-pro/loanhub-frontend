# Frontend env for local `npm run dev` (Vite reads this directory's .env).
# Copy to `frontend/.env`. For Docker, VITE_API_BASE_URL is passed as a build arg instead.

# Base URL of the Spring Boot API (context-path /api).
VITE_API_BASE_URL="http://localhost:8080/api"
